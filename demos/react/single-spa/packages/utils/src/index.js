import { 
  formatDate, 
  formatCurrency, 
  debounce, 
  throttle,
  deepClone,
  generateId,
  uniqueArray,
  storage,
  isValidEmail,
  isValidPhone,
  formatFileSize
} from './utils';

const MicroFrontendUtils = {
  formatDate,
  formatCurrency,
  debounce,
  throttle,
  deepClone,
  generateId,
  uniqueArray,
  storage,
  isValidEmail,
  isValidPhone,
  formatFileSize
};

export default MicroFrontendUtils;
export { 
  formatDate, 
  formatCurrency, 
  debounce, 
  throttle,
  deepClone,
  generateId,
  uniqueArray,
  storage,
  isValidEmail,
  isValidPhone,
  formatFileSize
};

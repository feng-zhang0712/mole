import {
  debounce,
  useDebounce,
  useDebounceFn,
} from './utils/debounce';
import {
  throttle,
  useThrottle,
  useThrottleFn,
} from './utils/throttle';

function App() {
  return (
    <div>
      
    </div>
  );
}

export default App;
